# Trivy vulnerability exceptions
# Gloria — 29 Mar 2026
#
# I've gone through the scan output and these are the ones we're knowingly
# skipping. Either there's no fix yet or it doesn't apply to how we run things.
#
# Rule of thumb: if ignore-unfixed catches it automatically, it won't appear
# here. This file is for anything we've manually reviewed and decided to accept.

# node:24-alpine ships with some musl/libssl findings that have no upstream fix
# as of today. The app doesn't expose these paths externally so the risk is low.
# Revisit this when the base image gets an update.

# CVE-XXXX-XXXXX  # example — replace with real ones after first scan run
